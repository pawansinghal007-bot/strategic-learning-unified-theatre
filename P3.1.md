Project: strategic-learning-unified-theatre at E:\VS Code Agent\Solution

Snapshot: Sprint 13 active. P0, P1, P2, P3 complete. Tests green.

Rules:
- ESM only, Node 18+, vitest, no build step
- Minimum-token execution, work locally only, no cloud API calls
- Scope limited to documentation hygiene, DB-first state policy,
  and master-instruction cleanup
- Do not design the full professional documentation structure yet

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE: P3.1 — Snapshot and Instruction Cleanup
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## Scope Note — record this in the reference document before executing

P3.1 is additional scope added to Sprint 13 beyond the original P0–P4 execution
plan. It was approved after P3 completed because the master instructions file had
grown to ~900 lines and was actively degrading handoff quality.

Before executing any step below, append this block to
`Solution/sprints/SPRINT-13-EXECUTION-PLAN.md` under a new heading
`## P3.1 — Added Scope (post-P3 approval)`:

```markdown
## P3.1 — Added Scope (post-P3 approval)

**Approved**: 2026-05-24, after P3 completion
**Rationale**: Master instructions file had grown to ~900 lines. Dynamic sprint
state was being appended mid-sprint, making the file slow to load and unreliable
as a stable architecture reference. P3.1 was added to enforce a DB-first state
policy and reduce the file to a stable, audit-ready document.

**In scope**:
- Insert DB-first workflow policy into master instructions
- Move dynamic sprint state out of master file
- Add continuous documentation maintenance rule
- Add Completed Sprints summary table
- Update Module Map and Data Locations for Sprint 13 deliverables
- Replace End-of-Sprint Checklist with DB-first version
- Enforce 400-line ceiling going forward

**Out of scope**:
- Full documentation system redesign
- New product docs
- Feature code, architectural refactors, new commands, infrastructure changes

**Outcome target**: Master file under 400 lines, stable between sprints,
with DB as live state store and snapshot command as session context loader.
```

Confirm this block is written to SPRINT-13-EXECUTION-PLAN.md before
proceeding to Step 0.

---

Goal: Make the master instructions file stable and stop it growing.

---

## Read first (in this order)

- E:\VS Code Agent\strategic-learning-unified-theatre-master-instructions.md
- Solution\sprints\SPRINT-13-EXECUTION-PLAN.md   ← confirm P3.1 block was just added
- src/ai-memory/memory-db.js
- src/commands/llm.js
- src/cli.js
- vitest.config.js       ← check for integration test separation added in P3

Before editing, answer from reading:
1. Which tables already exist for sprint_state, lessons, decisions, test_baselines?
2. Which CLI commands already exist for writing/reading those tables?
3. Which sections of the master file are dynamic sprint state vs stable architecture?
4. Is there already a DB-first policy paragraph?
5. Does vitest.config.js now have a test:integration separation from P3?
6. Confirm: is the P3.1 scope block now present in SPRINT-13-EXECUTION-PLAN.md?

---

## STEP 0 — Verify PATH before any CLI commands

```powershell
strategic-learning-unified-theatre --version
```

If this fails, run `npm link` from `E:\VS Code Agent\Solution` before
proceeding. Do not run Step 7 CLI commands if the tool is not on PATH.

---

## STEP 1 — Classify master file content

Go through every section. Produce a working-notes table only — do not edit yet.

| Section heading | Classification | Action |
|---|---|---|

Definitions:
- STABLE: architecture, module map, coding rules, data locations,
  constraints, configuration reference, command conventions
- DYNAMIC: active sprint status, test baselines, sprint history,
  task lists, completed deliverable checklists, what-changed logs,
  sprint prompts, lessons learned lists
- ARCHIVE: completed sprint summaries already superseded

---

## STEP 2 — Insert State Management Policy

Find "Recommended Next Architecture: Persistent AI Project Memory"
or the nearest equivalent heading.

Replace that section with this exact block — do not rephrase any line:

```markdown
## State Management Policy — Permanent Rule

### Source of truth during a sprint
The database is the single source of truth for all active sprint state.
Write the following there as they happen — never into this file mid-sprint:
- Active sprint status and current goal
- Blockers and their resolution
- Architectural decisions (title, rationale, outcome)
- Lessons learned (problem, fix, prevention rule)
- Test baselines (date, passing count, failing count, notes)
- Handoff notes and resume prompts

CLI commands for writing state during a sprint:
  strategic-learning-unified-theatre ai decisions add "<title>: <decision>"
  strategic-learning-unified-theatre ai lessons add "<problem> → <fix> → <rule>"
  strategic-learning-unified-theatre handoff update <id> --tokens-used <n>

### Source of truth at sprint close
Update this file exactly once per sprint, at close only.
The sprint-close update must contain:
  1. One-line sprint summary added to the Completed Sprints table
  2. Final test baseline (passing/total, date)
  3. Any durable architectural change to the Architecture or Module Map sections
  4. Any new constraint or rule that applies permanently to all future sprints

Do not append sprint task lists, tick-boxes, "What Changed" logs, or
per-sprint lesson lists to this file. Those live in the database.

### Snapshot rule
Every handoff snapshot must reference the DB sprint ID, not copy prose
from this file. Use:
  strategic-learning-unified-theatre ai snapshot
to generate the compact context block for the next session.

### Continuous documentation rule
Documentation is maintained continuously. Active sprint state, lessons,
decisions, and test baselines live in the database during the sprint.
The master instructions file is updated at sprint close only with durable
architecture, stable rules, and audit-relevant summaries. Product-facing
documentation should be maintained in dedicated docs and refreshed
whenever durable outcomes change.

### Enforcement
If this file grows beyond 400 lines, the next sprint's first task is cleanup.
Current line count must be checked and recorded at every sprint close.
```

---

## STEP 3 — Add Completed Sprints table

Find or create section `## Completed Sprints`.
Place it: after Vision Statement, before Architecture diagram.

Columns exactly:
| Sprint | Theme | Outcome | Test baseline | Date |

Use these verified rows — baselines sourced from master file sprint history:

| Sprint 7    | Response Feedback Loop                        | Complete | 118/118 | 2026-05-20 |
| Sprint 8    | Conversation Threading                        | Complete | 99/99   | 2026-05-20 |
| Sprint 9    | Self-Prompt Enhancement Loop                  | Complete | 118/118 | 2026-05-20 |
| Sprint 10   | Knowledge Graph + VS Code Ext                 | Complete | 139/139 | 2026-05-20 |
| Sprint 11   | Embedded Browser + Passive Capture            | Complete | 139/139 | 2026-05-20 |
| Sprint 12   | VS Code Passive Learning                      | Complete | 244/244 | 2026-05-21 |
| Sprint 12.1 | Browser Capture v2 / Native Host              | Complete | 263/264 | 2026-05-24 |
| Sprint 13   | Browser-to-Experience Bridge + LoRA Readiness | Complete | 264/264 | 2026-05-24 |

If you find a conflicting number in the file, use the file's number and flag
with a comment. Do not use — for rows where a baseline is provided above.

---

## STEP 4 — Remove or condense dynamic content

After Step 3, remove or condense sections classified DYNAMIC or ARCHIVE.

Remove:
- Sections now covered by Completed Sprints table
- All "What Changed Since Last Update" sections
- Per-sprint task checklists and tick-box deliverable lists
- Sprint-specific "Immediate Next Steps" lists
- Sprint-specific prompt examples (Example Sprint 13 Prompt, Example Sprint 14 Prompt)
  → replace with this single generic template block:

```markdown
## How to Start a New Agent Session

Run this first to get compact context without reloading this file:
  strategic-learning-unified-theatre ai snapshot

Or paste this block at the top of your prompt:

  Project: strategic-learning-unified-theatre at E:\VS Code Agent\Solution
  Instructions: Read E:\VS Code Agent\Solution\docs\README.md before writing any code.
  Snapshot: <paste output of: strategic-learning-unified-theatre ai snapshot>
  Current priority: [PASTE SPRINT GOAL HERE]
  Active sprint: [PASTE FROM: strategic-learning-unified-theatre handoff list]
  Use Context7 to resolve library IDs before generating code; place `use context7` at end.
```

- Duplicate content: keep the cleaner instance, remove the other

Keep without modification:
- Vision Statement
- Architecture diagram
- Module Map (update in Step 5)
- Configuration Reference
- Data Locations (update in Step 5)
- Constraints
- State Management Policy (just inserted)
- Completed Sprints table (just inserted)
- End-of-Sprint Checklist (replace in Step 6)
- Windows PowerShell Execution Rules
- Dependency Policy

If unsure about any section: keep it and add `<!-- REVIEW: candidate for removal -->`

---

## STEP 5 — Update Module Map and Data Locations

In Module Map, add if missing:
| `src/commands/bc2-sync.js`     | BC2 to experience.db bridge — syncs capture.db chat messages |
| `src/llm/training-exporter.js` | JSONL export of prompt/response pairs from experience.db |

In Data Locations, add if missing:
| Training exports | `~/.vscode-rotator/training-exports/` |

---

## STEP 6 — Replace End-of-Sprint Checklist

Replace the existing checklist with exactly this:

```markdown
## End-of-Sprint Checklist

Before ending any agent session:

1. `npm test` — confirm no new failures
2. `strategic-learning-unified-theatre ai decisions add "<decision>"` — record arch decisions
3. `strategic-learning-unified-theatre ai lessons add "<lesson>"` — record prevention rules
4. `strategic-learning-unified-theatre handoff update <id> --tokens-used <n>`
5. `strategic-learning-unified-theatre handoff close <id> --status paused|complete`
6. `strategic-learning-unified-theatre ai snapshot` — generate compact context for next session
7. Update this file at sprint close only:
   - Add one row to Completed Sprints table
   - Update test baseline in Test Status section
   - Update Module Map and Data Locations if architecture changed
   - Do not append sprint task lists or lesson logs
8. Count lines in this file:
   (Get-Content "E:\VS Code Agent\strategic-learning-unified-theatre-master-instructions.md").Count
   Target: under 400 lines. If over, add P0 cleanup task to next sprint.
```

---

## STEP 7 — Validate and close

Run in two separate blocks. Do not proceed to Block B if Block A has failures.

### Block A — PowerShell only
```powershell
cd "E:\VS Code Agent\Solution"
npm test

(Get-Content "E:\VS Code Agent\strategic-learning-unified-theatre-master-instructions.md").Count
```

Record: passing count, line count before this session, line count after.

### Block B — CLI only (requires PATH verified in Step 0)
```powershell
strategic-learning-unified-theatre ai snapshot

strategic-learning-unified-theatre ai lessons add "Master instruction files grow uncontrollably when used as sprint logs. Use DB for active state, update the instruction file once at sprint close only. Enforce a line-count ceiling to make drift visible."

strategic-learning-unified-theatre ai decisions add "Sprint 13 P3.1: DB-first state policy adopted permanently. P3.1 recorded as added scope in SPRINT-13-EXECUTION-PLAN.md. Master file updated once at sprint close. Target ceiling 400 lines. Snapshot command replaces file re-read for session context."

strategic-learning-unified-theatre handoff update <sprint-id> --tokens-used <n>

strategic-learning-unified-theatre handoff close <sprint-id> --status complete
```

---

## Acceptance criteria
- [ ] P3.1 scope block written to SPRINT-13-EXECUTION-PLAN.md before any other step
- [ ] SPRINT-13-EXECUTION-PLAN.md read back to confirm block is present
- [ ] State Management Policy inserted verbatim into master file
- [ ] Completed Sprints table has 8 rows, no — for provided baselines
- [ ] Dynamic sprint content removed or condensed
- [ ] Generic session template replaces sprint-specific prompt examples
- [ ] Module Map includes bc2-sync.js and training-exporter.js
- [ ] Data Locations includes training-exports path
- [ ] End-of-Sprint Checklist updated to DB-first workflow with line-count step
- [ ] Master file line count lower than before this session
- [ ] npm test green — no regressions
- [ ] Sprint 13 handoff closed in DB

---

## Do not
- Skip writing the P3.1 scope block to SPRINT-13-EXECUTION-PLAN.md
- Add new sprint task content to the master file
- Remove stable architecture sections
- Use — for baselines already provided in this prompt
- Start new feature work
- Modify bc2-sync, training-exporter, or any test files
- Run Block B before Block A is green

---

## Exit format

```
P3.1 CLOSED — Sprint 13 Complete
─────────────────────────────────────────────────
Scope recorded  : P3.1 block added to SPRINT-13-EXECUTION-PLAN.md ✅
Policy added    : State Management Policy ✅
Sprints table   : 8 rows ✅
Lines before    : <count>
Lines after     : <count>
Reduction       : <delta> lines removed
Module Map      : bc2-sync.js + training-exporter.js ✅
Data Locations  : training-exports ✅
Checklist       : DB-first workflow + line-count step ✅
Session template: Generic template replacing sprint-specific examples ✅
Tests           : <count>/264 passing ✅
Sprint 13       : Closed in handoff DB ✅
Lesson recorded : ✅
Next sprint     : Sprint 14 — start with: strategic-learning-unified-theatre ai snapshot
─────────────────────────────────────────────────
```

use context7
```

---

Three things changed from the previous version:

**The P3.1 scope block is now Step 0 of execution, not a header note.** The previous prompt had the scope explanation as a preamble that an agent could read and skip. Now it's a concrete write operation — append to `SPRINT-13-EXECUTION-PLAN.md` — that must complete and be read back before anything else runs. This makes the scope addition auditable and traceable in the sprint record.

**The read list now includes `SPRINT-13-EXECUTION-PLAN.md`** with an explicit check question: "Confirm P3.1 block is now present." This closes the loop between writing and verifying the scope record.

**The decision log command in Block B references P3.1 explicitly** — `"Sprint 13 P3.1: DB-first state policy adopted permanently. P3.1 recorded as added scope in SPRINT-13-EXECUTION-PLAN.md."` — so the DB record and the execution plan are cross-referenced and traceable from either direction.