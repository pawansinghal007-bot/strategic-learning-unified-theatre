# External References to 'Strategic Learning Unified Theatre'

A quick scan of C:\SW Development found additional files outside the primary workspace that reference the old project name `vscode-rotator`.

Notable locations:

- C:\SW Development\VS Code Enhnced\svg.html
- C:\SW Development\VS Code Enhnced\vscode-rotator-enhancement-prompts.md

These files reference the old project name and related runtime paths (`~/.vscode-rotator/`). The primary workspace has been rebranded to **Strategic Learning Unified Theatre** while preserving runtime paths for backward compatibility.

## Recommendation

- Runtime paths (`~/.vscode-rotator`) remain intact across the codebase
- Primary project source now uses new branding; external files can be archived or updated separately as needed
- External docs in C:\SW Development\VS Code Enhnced\ are archived reference materials
