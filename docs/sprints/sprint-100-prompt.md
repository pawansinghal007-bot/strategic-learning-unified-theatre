# Sprint 100 Prompt

## Objective

Complete the docs/contract sprint work that wires the AGENTS boot contract into the repo and refreshes the architecture summary without changing source behavior.

## Scope

1. The master timeline file is confirmed as master_timeline_sprints_1_97.md
   (only this file exists — do not create or reference a \_1_99 variant).
   Append a Sprint 100 entry summarizing this sprint's actual scope:
   AGENTS.md boot-contract creation, Standing Rules doc, Build State doc,
   copilot-instructions.md merge, llama.cpp harness prefix file, and
   PROJECT_ARCHITECTURE_AI_CONTEXT.md refresh (Sprint 28-era content
   replaced with current MCP/agents/LLM/security state). Do not touch
   entries for prior sprints. Do not rename the file, regardless of the
   sprint number now being 100 — this filename is permanent per Standing
   Rules.

2. Cat the FULL content of CURRENT_ACTIVE_SNAPSHOT.md before editing
   (previous inventory did not show its content clearly — confirm
   whether it's empty or has content). Report it in full. Then update
   it to point to strategic-learning-unified-theatre-ai-snapshot-sprint100-stable,
   preserving existing structure if content exists, or establishing a
   minimal pointer format if the file is empty.

3. Create a new snapshot file (no .md extension, matching existing
   convention) named exactly:
   strategic-learning-unified-theatre-ai-snapshot-sprint100-stable
   ...

4. Create or confirm AGENTS.md with the boot-contract guidance.
5. Create or update docs/standing-rules.md and docs/build-state.md.
6. Merge the AGENTS contract pointer into .github/copilot-instructions.md without removing existing content.
7. Create docs/llama-harness-prefix.md for the llama.cpp harness system-prompt prefix.
8. Refresh PROJECT_ARCHITECTURE_AI_CONTEXT.md to reflect the current MCP/agents/LLM/security architecture.

## Constraints

- Do not modify source code files.
- Preserve existing file structure and headings where still valid.
- Use the current repo state and standing rules as the source of truth.
- Keep confidence tagging explicit where intent is inferred rather than directly documented.
