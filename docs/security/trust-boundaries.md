# Trust Boundaries

## Purpose
Capture the system's trust and safety boundaries.

## Key boundaries
- `Solution` repo code is local-first and does not send data to external cloud APIs.
- The experience database is local and controlled by the development environment.
- Exported data is only prepared for future review or offline fine-tuning.

## Notes
Explicitly document that this project has no external API dependencies in its current design.
